[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "scratchml"
version = "0.1.0"
description = "Machine learning from scratch library"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
